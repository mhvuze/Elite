USE VISUAL Studio 2015 to BUILD.

BUILD IN DEBUG! RELEASE Will not work (app starts and closes for some reason, on some machines).
Go to "Project(R_Click)->Store->Create App Packages" to generate the .appx
Use the powerSheel script generated to import the certificate + app.